---
interface Props {
  kicker?: string;
  title: string;
  class?: string;
}
const { kicker, title, class: cls = "" } = Astro.props;
---
<header class:list={["page-head", cls]}>
  <div class="page-head-copy">
    {kicker && <p class="kicker">{kicker}</p>}
    <h1 class="h-section mt-3">{title}</h1>
    <div class="mt-5 lede measure"><slot /></div>
  </div>
  <div class="page-head-media"><slot name="media" /></div>
</header>

<style>
  .page-head {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    gap: 2.5rem;
    padding-block: 3.5rem 2.5rem;
    align-items: center;
  }
  .page-head-media:empty { display: none; }
  .page-head-media { display: grid; justify-items: end; }
  .page-head-media > :global(*) { width: 100%; }
  .page-head:has(.page-head-media:empty) { grid-template-columns: minmax(0, 1fr); }
  @media (min-width: 900px) {
    .page-head { grid-template-columns: minmax(0, 5fr) minmax(0, 7fr); gap: 3.5rem; padding-block: 4.5rem 3rem; }
  }
</style>
